apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cm-logs
  namespace: argocd
spec:
  project: cm
  source:
    repoURL: https://github.com/KiraPixel/lksp-infra.git
    targetRevision: HEAD
    path: manifests/cm-logs
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: fluentbit
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
